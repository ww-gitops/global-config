patches:
  - patch: |
      apiVersion: v1
      kind: ServiceAccount
      metadata:
        name: source-controller
        annotations:
          eks.amazonaws.com/role-arn: ${role_arn}      
    target:
      kind: ServiceAccount
      name: source-controller
