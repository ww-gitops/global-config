apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: GitRepository
metadata:
  name: clusters
  namespace: ${templateNamespace}
spec:
  interval: 10m
  ref:
    branch: main
  url: https://github.com/${GitHubOrg}/clusters.git
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: ${resourceName}-boot
  namespace: ${templateNamespace}
spec:
  prune: true
  wait: true
  timeout: 5m
  interval: 5m
  sourceRef:
    kind: GitRepository
    name: clusters
  path: ./cluster-yamls/s3-src
  postBuild:
    substituteFrom:
      - kind: ConfigMap
        name: cluster-config
